---
title: Index page
---
{% extends "layouts/_layout.njk" %}

{% block content %}

    {# variable example #}
    <h1>{{title}}</h1>

    <% if(svgSprites || pngSprites){ %>{# mixin example #}<%}%>
    <% if(svgSprites){ %>{{mixins.iconSvgMono('home-mono')}}
    {{mixins.iconSvgMulti('home-multi')}}<%}%>
    <% if(pngSprites){ %>{{mixins.iconPng('alarm')}}
    {{mixins.iconPng('smile')}}<%}%>

{% endblock %}